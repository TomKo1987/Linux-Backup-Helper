[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "linux-backup-helper"
version = "1.0.0"
description = "Graphical backup and system management tool for Linux"
readme = "README.md"
license = { file = "LICENSE.txt" }
requires-python = ">=3.10"
dependencies = [
    "PyQt6>=6.4,<7",
    "keyring>=23.0,<26",
    "secretstorage>=3.3,<4",
]

[project.scripts]
backup-helper = "main:main"

[tool.setuptools]
py-modules = [
    "advanced_copy",
    "backup_lock",
    "backup_stats",
    "constants",
    "copy_worker",
    "copy_worker_core",
    "copy_worker_gui",
    "copy_worker_smb",
    "dialog_base",
    "disk_analyzer",
    "dotfiles_manager",
    "drive_utils",
    "dry_run",
    "entry_dialog",
    "firewall_rules",
    "header_settings_dialog",
    "history",
    "icons",
    "integrity_checker",
    "linux_distro_helper",
    "main",
    "misc_dialogs",
    "mount_dialogs",
    "pre_post_hooks",
    "profile_compare",
    "profiles_dialog",
    "samba_credentials",
    "scan_verify",
    "scan_verify_capture",
    "scan_verify_helpers",
    "scan_verify_packagediff",
    "scan_verify_verify",
    "scheduler",
    "state",
    "status_panel",
    "sudo_password",
    "system_manager",
    "system_manager_dotfiles_editor",
    "system_manager_helpers",
    "system_manager_ops_editor",
    "system_manager_options",
    "themes",
    "tooltips",
    "translations",
    "ui_utils",
    "windows",
]

[tool.setuptools.package-data]
"*" = ["images/*.png"]
